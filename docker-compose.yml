services:
  web:
    build:
      context: .
      args:
        YANDEX_MAPS_BROWSER_ARG: ${VITE_YANDEX_MAPS_API_KEY:-}
    ports:
      - "${PORT:-3000}:3000"
    env_file: .env
    volumes:
      - grassigrosso-data:/app/data
    restart: unless-stopped

volumes:
  grassigrosso-data:
