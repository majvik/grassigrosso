---
phase: 01-shared-page-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - strapi-catalog/src/components/page/list-item.json
  - strapi-catalog/src/components/page/faq-item.json
  - strapi-catalog/src/components/page/hero.json
  - strapi-catalog/src/components/page/section.json
  - strapi-catalog/src/admin/translations/ru.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "В Strapi доступны shared компоненты page.hero, page.section, page.faq-item, page.list-item"
    - "У полей этих компонентов русские лейблы (CM + CTB ключи + displayName), не сырые английские ключи"
    - "page.section вкладывает repeatable page.list-item без ошибок схемы при bootstrap / strapi develop"
    - "В diff Phase 1 нет single types, feeds, frontend, seed"
  artifacts:
    - path: "strapi-catalog/src/components/page/hero.json"
      provides: "page.hero — title/description/image/image_alt/cta_label/cta_url"
      contains: "components_page_heroes"
    - path: "strapi-catalog/src/components/page/section.json"
      provides: "page.section с nested items → page.list-item"
      contains: "page.list-item"
    - path: "strapi-catalog/src/components/page/faq-item.json"
      provides: "page.faq-item — question/answer/open_by_default"
      contains: "components_page_faq_items"
    - path: "strapi-catalog/src/components/page/list-item.json"
      provides: "page.list-item — title/text/href"
      contains: "components_page_list_items"
    - path: "strapi-catalog/src/admin/translations/ru.json"
      provides: "CM + CTB RU labels для всех page.* атрибутов"
      contains: "content-manager.components.page.hero.title"
  key_links:
    - from: "strapi-catalog/src/components/page/section.json"
      to: "strapi-catalog/src/components/page/list-item.json"
      via: "attributes.items.component"
      pattern: "\"component\":\\s*\"page\\.list-item\""
    - from: "strapi-catalog/src/admin/translations/ru.json"
      to: "strapi-catalog/src/components/page/*.json"
      via: "content-manager.components.page.* + content-type-builder.components.page.*.attributes.*"
      pattern: "content-manager\\.components\\.page\\.(hero|section|faq-item|list-item)\\."
    - from: "strapi-catalog/scripts/prepare-dist.cjs"
      to: "dist/src/components/page/"
      via: "copy src/components on Strapi boot"
      pattern: "components"
---

<objective>
Создать четыре переиспользуемых Strapi-компонента категории `page` (`hero`, `section`, `faq-item`, `list-item`) со схемами из RESEARCH и полными русскими лейблами в `ru.json`, чтобы Phase 2 могла собирать wave-1 single types без английских fallback.

Purpose: Закрыть ADM-01 и критерии успеха Phase 1 (компоненты в CTB, RU labels, nestability через section→list-item).
Output: `strapi-catalog/src/components/page/*.json` + обновлённый `ru.json`; Strapi boot без ошибок схемы; без single types / feeds / frontend / seed.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shared-page-components/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@strapi-catalog/src/components/catalog/hero-slide.json
@strapi-catalog/src/components/catalog/filter-help-summary-item.json
@strapi-catalog/src/admin/translations/ru.json
@strapi-catalog/src/admin/app.js
@strapi-catalog/scripts/prepare-dist.cjs

Authoritative schemas and RU label table: `01-RESEARCH.md` § Exact Component Schemas + § Phase 1 keys to add.
Mirror empty `"options": {}` like catalog components. English snake_case attribute keys. Russian `info.displayName` + attribute `displayName` + CM/CTB `ru.json` pairs.

**Out of scope (do NOT create):**
- `strapi-catalog/src/api/*-page/` single types
- feeds, `server.cjs` `/api/pages/*`, snapshots
- React hydrate, seed in `strapi-catalog/src/index.js`
- Changes to `catalog.*` components
- Index video fields on hero (Phase 2)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create leaf page components (list-item, faq-item)</name>
  <files>
    strapi-catalog/src/components/page/list-item.json
    strapi-catalog/src/components/page/faq-item.json
  </files>
  <action>
    Create directory `strapi-catalog/src/components/page/` if missing.

    **`list-item.json`** — UID `page.list-item`:
    - collectionName: `components_page_list_items`
    - info.displayName: `Пункт списка`, icon: `bulletList`
    - options: `{}`
    - attributes (English keys + RU displayName on each):
      - `title` string required — displayName `Заголовок`
      - `text` text optional — displayName `Текст`
      - `href` string optional — displayName `Ссылка`
    - Do NOT add icon/media/type/size fields (page-specific → Phase 2)

    **`faq-item.json`** — UID `page.faq-item`:
    - collectionName: `components_page_faq_items`
    - info.displayName: `Пункт FAQ`, icon: `question`
    - options: `{}`
    - attributes:
      - `question` string required — displayName `Вопрос`
      - `answer` text required — displayName `Ответ`
      - `open_by_default` boolean optional, default `false` — displayName `Открыт по умолчанию`
    - No FAQ section title here (lives on single type in Phase 2)
    - No enums, no draftAndPublish
  </action>
  <verify>
    `test -f strapi-catalog/src/components/page/list-item.json && test -f strapi-catalog/src/components/page/faq-item.json`
    `python3 -c "import json; json.load(open('strapi-catalog/src/components/page/list-item.json')); json.load(open('strapi-catalog/src/components/page/faq-item.json'))"`
    `rg -n '"collectionName"|"displayName"|"open_by_default"' strapi-catalog/src/components/page/list-item.json strapi-catalog/src/components/page/faq-item.json`
  </verify>
  <done>
    Both leaf JSON schemas valid; UIDs implied by paths `page.list-item` / `page.faq-item`; required fields match RESEARCH tables.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create page.hero and page.section (nests list-item)</name>
  <files>
    strapi-catalog/src/components/page/hero.json
    strapi-catalog/src/components/page/section.json
  </files>
  <action>
    **`hero.json`** — UID `page.hero`:
    - collectionName: `components_page_heroes`
    - info.displayName: `Hero страницы`, icon: `landscape`
    - options: `{}`
    - attributes:
      - `title` string required — `Заголовок`
      - `description` text optional — `Описание`
      - `image` media single, allowedTypes `["images"]`, optional — `Изображение`
      - `image_alt` string optional — `Alt изображения`
      - `cta_label` string optional — `Текст кнопки`
      - `cta_url` string optional — `Ссылка кнопки`
    - Do NOT add video_desktop / video_mobile / poster (Index-only → Phase 2 single type)

    **`section.json`** — UID `page.section` (nestability proof for success criterion 3):
    - collectionName: `components_page_sections`
    - info.displayName: `Секция`, icon: `layer`
    - options: `{}`
    - attributes:
      - `title` string required — `Заголовок`
      - `subtitle` text optional — `Подзаголовок`
      - `body` text optional — `Текст`
      - `items`: type `component`, repeatable `true`, component `"page.list-item"`, displayName `Пункты`
    - Do NOT nest section inside itself or hero inside section
  </action>
  <verify>
    `python3 -c "import json; s=json.load(open('strapi-catalog/src/components/page/section.json')); assert s['attributes']['items']['component']=='page.list-item' and s['attributes']['items']['repeatable'] is True"`
    `python3 -c "import json; h=json.load(open('strapi-catalog/src/components/page/hero.json')); assert h['attributes']['image']['allowedTypes']==['images']; assert 'video_desktop' not in h['attributes'] and 'video_mobile' not in h['attributes']"`
    `ls strapi-catalog/src/components/page/ | sort` → exactly the four files: faq-item.json, hero.json, list-item.json, section.json
  </verify>
  <done>
    Four page components exist; `page.section.items` references `page.list-item` repeatable; hero is image+CTA only.
  </done>
</task>

<task type="auto">
  <name>Task 3: RU translations + Strapi boot / dist sync verify</name>
  <files>
    strapi-catalog/src/admin/translations/ru.json
  </files>
  <action>
    Append to `ru.json` (keep valid JSON; preserve existing keys) — dual CM + CTB for every attribute from RESEARCH table:

    For each of hero, section, faq-item, list-item and each attribute:
    - `content-manager.components.page.<name>.<attr>`
    - `content-type-builder.components.page.<name>.attributes.<attr>`

    RU strings (exact):
    | Component | attr | RU |
    | hero | title | Заголовок |
    | hero | description | Описание |
    | hero | image | Изображение |
    | hero | image_alt | Alt изображения |
    | hero | cta_label | Текст кнопки |
    | hero | cta_url | Ссылка кнопки |
    | section | title | Заголовок |
    | section | subtitle | Подзаголовок |
    | section | body | Текст |
    | section | items | Пункты |
    | faq-item | question | Вопрос |
    | faq-item | answer | Ответ |
    | faq-item | open_by_default | Открыт по умолчанию |
    | list-item | title | Заголовок |
    | list-item | text | Текст |
    | list-item | href | Ссылка |

    Also add displayName / category keys:
    - `"Hero страницы": "Hero страницы"`
    - `"Секция": "Секция"`
    - `"Пункт FAQ": "Пункт FAQ"`
    - `"Пункт списка": "Пункт списка"`
    - `"page": "Страница"`

    Do NOT invent single-type CM/CTB keys (ADM-04 → Phase 2).

    Then verify bootstrap path:
    1. Ensure JSON still parses: `python3 -c "import json; json.load(open('strapi-catalog/src/admin/translations/ru.json'))"`
    2. Run prepare-dist sync the same way Strapi does — e.g. `node strapi-catalog/scripts/prepare-dist.cjs` from `strapi-catalog` cwd (or trigger via short `npm run develop --prefix strapi-catalog` if that is the project habit). Confirm `dist/src/components/page/` contains the four JSON files after sync.
    3. Prefer a short boot check: start Strapi develop/start long enough to confirm no schema error mentioning `page.list-item` / `page.section`; then stop. If develop is heavy, at minimum: prepare-dist copy success + schema JSON asserts above.
    4. Scope guard: `git status` / `git diff --name-only` must NOT include new `strapi-catalog/src/api/*-page/`, `server.cjs` page routes, `src/components/pages/*`, or seed changes for pages.
  </action>
  <verify>
    `python3 -c "import json; d=json.load(open('strapi-catalog/src/admin/translations/ru.json')); keys=['content-manager.components.page.hero.cta_label','content-type-builder.components.page.section.attributes.items','content-manager.components.page.faq-item.open_by_default','content-manager.components.page.list-item.href','page']; assert all(k in d for k in keys), [k for k in keys if k not in d]"`
    `test -d strapi-catalog/dist/src/components/page && ls strapi-catalog/dist/src/components/page/` (after prepare-dist)
    `rg -l 'api/.*-page' strapi-catalog/src/api 2>/dev/null | rg 'hotels-page|dealers-page|contacts-page|documents-page|index-page' || true` → empty for new wave-1 page singles
    Count CM keys for page.*: expect 16 attribute CM keys + 16 CTB keys (+ displayName/category strings)
  </verify>
  <done>
    All page.* CM+CTB keys present with Russian values; dist sync includes `components/page/`; Strapi schema load does not error on nested `page.section`→`page.list-item`; Phase 1 diff is components + ru.json only.
  </done>
</task>

</tasks>

<verification>
- Four files under `strapi-catalog/src/components/page/` match RESEARCH attribute tables exactly.
- `page.section.attributes.items.component === "page.list-item"` and `repeatable: true`.
- `ru.json` has both `content-manager.components.page.*` and `content-type-builder.components.page.*.attributes.*` for all 16 attributes + category/displayName strings.
- After prepare-dist (or develop), `dist/src/components/page/*.json` exists.
- No new page single types, feeds, frontend hydrate, or page seed in this plan's changes.
</verification>

<success_criteria>
1. Shared components `page.hero`, `page.section`, `page.faq-item`, `page.list-item` exist as Strapi component schemas (CTB-ready after boot).
2. Field labels are Russian via schema `displayName` + complete CM/CTB `ru.json` keys (no reliance on raw English attr names for editors).
3. Nesting `page.section` → `page.list-item` is schema-valid without shipping wave-1 single types.
4. ADM-01 satisfied; ADM-02/03/04, API-*, FE-*, DEP-* untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-page-components/01-01-SUMMARY.md`
</output>
